{
  "name": "recruiter-app-server",
  "version": "1.0.0",
  "description": "Express server for Recruiter App",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "cd frontend && npm run build",
    "deploy": "npm run build && npm start"
  },
  "dependencies": {
    "body-parser": "^1.20.3",
    "express": "^4.21.2"
  },
  "engines": {
    "node": "16.x"
  }
}
